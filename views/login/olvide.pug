extends ../layout.pug

block contenido
    h1.text-center.mt-5.upTask SmartList.
    h3.text-center.lead.h1.text-white ¿Olvidaste tu contraseña? No te preocupes que puedes restablecerla de una manera muy sencilla.
    .container.mt-5
        .row 
            .col-md-6.col-xl-5.col-lg-5.mx-auto 
                form(action="/olvide", method="post", class="bg-form p-3 p-md-5 rounded shadow-sm")
                    h3.text-center.text-white.mb-3.texto  Restablecer contraseña.  
                    if(errores)
                       each error in errores
                            div(class=`alert alert-${error.tipo} alert-dismissible fade show`, role='alert')
                                |  #{error.mensaje}.
                                button.btn-close(type='button' data-bs-dismiss='alert' aria-label='Close')
                                
                    .form-floating.mb-3
                        input(type="email", name="correo", id="correo", class="form-control", placeholder="ejemplo@correo.com..")
                        label(for="correo") Correo electrónico  
                    input(type="submit", value="Enviar instrucciones", class="btn btn-primary btn-lg btn-block w-100")

                .mt-4.d-flex.justify-content-between.px-2
                    a(href="/", class="link-offset-2 link-underline link-underline-opacity-0 link-color") Inicia sesión
                    a(href="/registro", class="link-offset-2 link-underline link-underline-opacity-0 link-color") Crear cuenta 